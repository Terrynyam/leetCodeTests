package com.example.LeetCodeTest.cleancode;

public class UseCommentsWisely {
    /*Comments should explain why the code exists, not what it does (the code should be self-explanatory).
    Avoid redundant comments.*/

    /*Use comments to explain why something is done, not what is done.
    If code is self-explanatory, comments are not needed.*/

    //Bad Example
    // Increment i by 1
    // i = i + 1;

    // Bad example
// Increment the counter by 1
    //counter++;

    // Good example
// Retry the request after a network failure
    //retryRequest();

    //Good Example

    // Fetching latest data from the server because cache is outdated.
    // fetchLatestData();


}
