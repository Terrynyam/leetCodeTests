package com.example.LeetCodeTest.cleancode;

public class WriteUnitTests {
    /*Writing unit tests ensures code correctness and allows for safe refactoring.
        Follow the Arrange-Act-Assert pattern for readability.
        Unit tests ensure that code behaves as expected.
        Clean code is often accompanied by a solid test suite.*/

    /*@Test
    public void shouldReturnSumOfTwoNumbers() {
        Calculator calculator = new Calculator();
        int result = calculator.add(2, 3);
        assertEquals(5, result);
    }
    @Test
    public void testCalculateTotal() {
        Order order = new Order(List.of(new Item("Apple", 1.0), new Item("Orange", 2.0)));
        assertEquals(3.0, calculateTotal(order));
    }*/
}
